# Статус проекта PlatformLove

Обновлено: 2026-09-15

## Стек

- **Фронтенд**: React 19 + Vite 8 + Tailwind 4, React Router 7, Capacitor 8 (Android)
- **Бэкенд**: Node.js + Express 5, JWT, bcryptjs, multer, nodemailer
- **БД**: SQLite (`server/database.sqlite`), идемпотентная миграция `server/src/db/migrate.js`,
  демо-данные `server/scripts/seed.js`

## Команды

| Что | Команда |
| --- | --- |
| Фронтенд (dev) | `npm run dev` → порт **5175** |
| Сборка веба | `npm run build` |
| Линт | `npm run lint` |
| Бэкенд (dev) | `cd server && npm run dev` → порт **3001** |
| Миграция + демо-данные | `cd server && npm run db:init` / `node scripts/seed.js` |
| Синхронизация Android | `npx cap sync android` |

Vite проксирует `/api` и `/uploads` на `http://localhost:3001`, поэтому во фронтенде
используются относительные пути.

## Онбординг (первый запуск)

- Компонент: `src/OnboardingScreen.jsx`
- Изображения: `public/onboarding/1-flowers.jpg`, `2-love.jpg`, `3-friends.jpg`, `4-wedding.jpg`
  (попадают в `dist/` и в APK при сборке)
- 4 слайда со свайпом, snap-скроллом, точками-индикаторами, стрелкой «далее» и кнопкой «Пропустить».
  Последняя точка — «Старт». Поддержаны стрелки ←/→ на десктопе.
- Флаг прохождения: `localStorage.onboarding_done`. Пока он не выставлен, корневой маршрут `/`
  показывает онбординг вместо формы входа. Маршрут `/onboarding` доступен явно.
- После «Пропустить» / «Старт» выполняется `navigate("/", { replace: true })` → показывается вход.

## Авторизация

### Исправленный корневой баг

`src/api.js` кэшировал токен в конструкторе синглтона. После входа токен попадал в `localStorage`,
но экземпляр `api` продолжал слать запросы **без** `Authorization`, и все экраны после логина
работали как анонимные. Теперь токен читается актуально через геттеры `authToken` /
`authAdminToken` на каждый запрос.

Проверено headless-скриптом: запрос до логина идёт без заголовка, после логина заголовок
присутствует, `/api/auth/me` возвращает того же пользователя.

### Маршруты входа

| Сценарий | Поведение |
| --- | --- |
| Логин + пароль, PIN не задан | `/menu`, если анкета заполнена; иначе `/profile` |
| Логин + пароль, PIN задан | шаг 2 — ввод PIN, затем `/menu` |
| Регистрация | токен сохраняется, переход на `/profile` (анкета) |
| Установка PIN (`/set-pin`) | после сохранения → `/menu`; PIN опционален и не блокирует вход |
| Вход по СМС (`/verify`) | `send-code` → `verify-code` → `/profile` для новых, `/menu` для существующих |
| Выход | `api.logout()` очищает токен |

PIN больше не является обязательным барьером: раньше вход без PIN отправлял пользователя
в мастер «создай PIN → анкета», и в приложение он не попадал.

### Защита маршрутов

`RequireAuth` (layout-роут в `src/App.jsx`) разворачивает на `/` все экраны, требующие токена:
`/profile`, `/questions`, `/menu`, `/swipe`, `/wall`, `/match`, `/likes`, `/fans`,
`/notifications`, `/messages`, `/feed`, `/friends`, `/user-profile`, `/chat/:id`,
`/settings`, `/subscription`, `/blacklist`.

`RootGate` на `/`: онбординг → (есть токен ? `/menu` : форма входа).

### SMS-эндпоинты

Реализованы в `server/src/routes/auth.js` на существующей таблице `verification_codes`
(`id`, `phone`, `code`, `attempts`, `expires_at`, `verified`, `created_at`):

- `POST /api/auth/send-code` — генерирует 4-значный код, срок жизни 5 минут,
  инвалидирует предыдущие активные коды номера. В не-продакшене возвращает `debug_code`
  (реальная отправка SMS — за провайдером: SMS.Io / Twilio).
- `POST /api/auth/verify-code` — проверка с лимитом 5 попыток, одноразовость,
  создание пользователя при первом входе по номеру.

## Демо-доступы (только локальная база sandbox)

- Пользователь: `anna` / `demo1234`
- Админ: `admin@platformlove.ru` / `admin123`

Это учётки из `server/scripts/seed.js` для разработки. К боевому серверу
они отношения не имеют: там своя база с реальными пользователями, а админ
создан `create-admin.js` и пароль у него свой (см. `deploy/DEPLOY_INSTRUCTIONS.md`).

## Прод и деплой

- Боевой сервер — VPS **id 7924729** (`cv7924729.novalocal`), `80.78.244.136`, домен `platformlove.com`.
- Боевой каталог один: `/var/www/platformlove.com` (nginx → `dist`, `/api` → `127.0.0.1:3001`,
  systemd `platformlove-backend.service`). Вторая копия `/var/www/platform-love` с 0 юзерами
  отключена и переименована в `…DISABLED-2026-09-15`.
- Выкладка — `bash deploy/deploy.sh`: локальная сборка → бэкап БД и кода → перенос →
  миграция → рестарт → health-check → автооткат при провале. Подробности: `deploy/DEPLOY_INSTRUCTIONS.md`.
- `server/scripts/seed.js` на проде **не запускать** — он делает `DELETE FROM` по таблицам.
- Админ заводится `server/scripts/create-admin.js` (только таблица `admins`).
- Путь к БД переопределяется переменной `DATABASE_PATH` — так репетиция миграции
  прогоняется на копии боевых данных перед применением.
- Код синхронизируется с GitHub: `Xodesign/platform-love` (приватный), ветка `main`.

## Найдено и исправлено при аудите прода (15.09)

### Service worker ломал показ обновлений

Старый `public/sw.js` кэшировал `index.html` по принципу «сначала кэш» и содержал
захардкоженные имена бандлов прошлой сборки. После любого деплоя браузер отдавал
пользователю старый `index.html`, тот запрашивал уже несуществующие JS/CSS —
и приложение показывало пустой экран. Новое поведение:

- навигация и `index.html` — всегда сеть, кэш только как офлайн-запас;
- `/assets/*` — можно кэшировать (хэш содержимого в имени файла);
- `/api` и `/uploads` — не кэшируются никогда: это данные конкретных пользователей;
- `install` не падает из-за отсутствия одного файла, `activate` удаляет старые кэши
  и вызывает `clients.claim()`, чтобы новая версия вступила сразу.

Регистрация SW перенесена в `src/main.jsx` и работает только в production-сборке.
В разработке SW, наоборот, снимается, а кэши вычищаются — иначе правки «не применялись».

### Админка была доступна без входа

`/admin` открывался напрямую по URL: проверки сессии не было, а экраны при 401
дорисовывали демо-данные. Теперь `AdminLayout` сверяет токен с сервером через
`GET /api/admin/me` и при отказе редиректит на `/admin/login`. Локальный PIN
больше не заменяет серверную проверку: принимается только после подтверждения
email/пароля в этой сессии и хранится как SHA-256, а не открытым текстом.
Выход теперь действительно удаляет `admin_token`.

### Профиль жил только в браузере

Анкета, имя, город, фото и обложка сохранялись в `localStorage`, поэтому на другом
устройстве профиль был пустым, а в свайпах другим показывалась старая запись из базы.
Теперь сервер — источник истины, `localStorage` только кэш. Галерея хранит ссылки
`/uploads/...` вместо base64: двенадцать фото в base64 выбивали квоту `localStorage`,
и приложение ломалось. Заодно `uploadPhoto` читал устаревшее поле токена и уходил
без `Authorization`.

### Размер бандла

Админка вынесена в 16 ленивых чанков (`React.lazy` + `Suspense`): главный бандл
562 → 437 КБ, предупреждение Vite о чанке >500 КБ ушло.

## Известные ограничения

- Реальная отправка SMS и email не подключена (нужны ключи провайдера через env).
  В не-продакшене `send-code` возвращает `debug_code`, на проде (`NODE_ENV=production`) — нет.
- Платежи — демо-режим: `POST /api/subscriptions` записывает подписку без внешнего эквайринга.
- Экран анкеты (`/profile`, `/questions`) сохраняет черновик локально; ответы уходят в
  `/api/filters/answers`, но базовые поля профиля на сервер не пишутся.
- Часть таблиц админки и соц-данных наполняется сидом для демо.
