# Статус проекта PlatformLove

Обновлено: 2026-09-15 (аудит авторизации по логину + PIN)

## Стек

- **Фронтенд**: React 19 + Vite 8 + Tailwind 4, React Router 7, Capacitor 8 (Android)
- **Бэкенд**: Node.js + Express 5, JWT, bcryptjs, multer, nodemailer
- **БД**: SQLite (`server/database.sqlite`), идемпотентная миграция `server/src/db/migrate.js`,
  демо-данные `server/scripts/seed.js`

## Команды

| Что | Команда |
| --- | --- |
| Фронтенд (dev) | `npm run dev` → порт **5175** |
| Сборка веба | `npm run build` |
| Линт | `npm run lint` |
| Бэкенд (dev) | `cd server && npm run dev` → порт **3001** |
| Миграция + демо-данные | `cd server && npm run db:init` / `node scripts/seed.js` |
| Синхронизация Android | `npx cap sync android` |

Vite проксирует `/api` и `/uploads` на `http://localhost:3001`, поэтому во фронтенде
используются относительные пути.

## Онбординг (первый запуск)

- Компонент: `src/OnboardingScreen.jsx`
- Изображения: `public/onboarding/1-flowers.jpg`, `2-love.jpg`, `3-friends.jpg`, `4-wedding.jpg`
  (попадают в `dist/` и в APK при сборке)
- 4 слайда со свайпом, snap-скроллом, точками-индикаторами, стрелкой «далее» и кнопкой «Пропустить».
  Последняя точка — «Старт». Поддержаны стрелки ←/→ на десктопе.
- Флаг прохождения: `localStorage.onboarding_done`. Пока он не выставлен, корневой маршрут `/`
  показывает онбординг вместо формы входа. Маршрут `/onboarding` доступен явно.
- После «Пропустить» / «Старт» флаг выставляется и экран меняется на форму входа.
  Исправлено 15.09: раньше `complete()` делал только `navigate("/", { replace: true })`,
  а на корневом маршруте это тот же адрес — React Router не перерисовывал `RootGate`,
  и после нажатия «Пропустить» при первом запуске на экране оставался онбординг
  до ручной перезагрузки. Теперь `RootGate` держит флаг в `useState` и передаёт в
  `OnboardingScreen` колбэк `onFinish`; `navigate` остался для явного `/onboarding`.
  Проверено кликом в браузере на чистом профиле (порт 5176): «Пропустить» → форма входа.

## Авторизация

### Исправленный корневой баг

`src/api.js` кэшировал токен в конструкторе синглтона. После входа токен попадал в `localStorage`,
но экземпляр `api` продолжал слать запросы **без** `Authorization`, и все экраны после логина
работали как анонимные. Теперь токен читается актуально через геттеры `authToken` /
`authAdminToken` на каждый запрос.

Проверено headless-скриптом: запрос до логина идёт без заголовка, после логина заголовок
присутствует, `/api/auth/me` возвращает того же пользователя.

### Схема входа (актуальная)

СМС-входа в интерфейсе больше нет. Пользователь проходит один из путей:

| Сценарий | Поведение |
| --- | --- |
| Регистрация `/register` | имя + логин + **email (обязательно)** + пароль → токен → сразу `/set-pin` |
| Первый вход `/` шаг 1 | логин (или email) + пароль; PIN не задан → `/set-pin` |
| Обычный вход `/` шаг 2 | логин + пароль → экран «Введите PIN-код» → `/menu` (или `/profile`, если анкета пуста) |
| Установка PIN `/set-pin` | ровно 4 цифры, дважды; после сохранения → `/menu` или `/profile` |
| Забыли PIN или пароль `/forgot-password` | логин → код на email (6 цифр, 15 минут) → смена пароля и/или PIN |
| Привязка/смена почты `/email` | адрес → код на новый адрес (6 цифр, 15 минут, одноразовый) → `users.email` обновлён; доступно из Настроек и анкеты |
| Выход | `api.logout()`; на `/set-pin` есть «Выйти и войти позже» |

`localStorage.pin_login` запоминает логин: при следующем открытии экран входа
сразу начинается с шага 2 (ввод PIN), пароль вводить не нужно.

### Обязательность PIN и защита маршрутов

`RequireAuth` (layout-роут в `src/App.jsx`) разворачивает на `/` все экраны, требующие токена:
`/profile`, `/questions`, `/menu`, `/swipe`, `/wall`, `/match`, `/likes`, `/fans`,
`/notifications`, `/messages`, `/feed`, `/friends`, `/user-profile`, `/chat/:id`,
`/settings`, `/subscription`, `/blacklist`.

Два уровня проверки PIN на клиенте:

- `RootGate` на `/`: онбординг → (есть токен ? (`hasPin === false` → `/set-pin`, иначе `/menu`) : форма входа);
- `RequireAuth` дополнительно разворачивает на `/set-pin` любой защищённый экран, если у
  сохранённого пользователя `hasPin === false`. До правки 15.09 проверка жила только на `/`,
  и не дошедший до PIN пользователь мог открыть `/menu` напрямую по URL.

**Важно:** это только клиентский барьер. Сервер PIN не требует — любой запрос с валидным
JWT проходит независимо от наличия PIN. Жёсткая серверная проверка сейчас заблокировала бы
существующие аккаунты без PIN (на проде их большинство), поэтому она не включена.

### Проверка флоу (смоук-тест)

`node server/scripts/smoke-auth-pin.mjs [base_url]` — 16 проверок реального HTTP-флоу
против запущенного бэкенда: отказ регистрации без email, `requiresPin`, вход по email
вместо логина, хеш PIN в БД (не открытый текст), неверный/верный PIN, `forgot-password`
→ код в `password_reset_codes` → смена только PIN, одноразовость кода, `/me`.
Тестовая учётка `smoke*` создаётся и удаляется самим скриптом; пароль генерируется
на ходу и в репозиторий не попадает. Логины/пароли боевой базы скрипт не трогает.

### SMS-эндпоинты (наследие, в интерфейсе не используются)

Экрана входа по СМС в приложении нет, но маршруты в `server/src/routes/auth.js` остались:

- `POST /api/auth/send-code` — 4-значный код, срок жизни 5 минут, лимит 5 попыток;
  в не-продакшене возвращает `debug_code` (проверено на проде: `NODE_ENV=production`,
  кода в ответе нет);
- `POST /api/auth/verify-code` — проверка с лимитом попыток, одноразовость,
  **создание аккаунта без email и пароля** при первом входе по номеру.

Второй пункт — лишний вход в обход схемы «email обязателен»: такой аккаунт нельзя
восстановить. Маршруты стоит удалить или закрыть фича-флагом; пока это мёртвый код.
Реальной отправки SMS нет: `SMSC_*` в env не подключены к провайдеру.

## Демо-доступы (только локальная база sandbox)

- Пользователь: `anna` / `demo1234`
- Админ: `admin@platformlove.ru` / `admin123`

Это учётки из `server/scripts/seed.js` для разработки. К боевому серверу
они отношения не имеют: там своя база с реальными пользователями, а админ
создан `create-admin.js` и пароль у него свой (см. `deploy/DEPLOY_INSTRUCTIONS.md`).

## Прод и деплой

- Боевой сервер — VPS **id 7924729** (`cv7924729.novalocal`), `80.78.244.136`, домен `platformlove.com`.
- Боевой каталог один: `/var/www/platformlove.com` (nginx → `dist`, `/api` → `127.0.0.1:3001`,
  systemd `platformlove-backend.service`). Вторая копия `/var/www/platform-love` с 0 юзерами
  отключена и переименована в `…DISABLED-2026-09-15`.
- Выкладка — `bash deploy/deploy.sh`: локальная сборка → бэкап БД и кода → перенос →
  миграция → рестарт → health-check → автооткат при провале. Подробности: `deploy/DEPLOY_INSTRUCTIONS.md`.
- `server/scripts/seed.js` на проде **не запускать** — он делает `DELETE FROM` по таблицам.
- Админ заводится `server/scripts/create-admin.js` (только таблица `admins`).
- Путь к БД переопределяется переменной `DATABASE_PATH` — так репетиция миграции
  прогоняется на копии боевых данных перед применением.
- Код синхронизируется с GitHub: `Xodesign/platform-love` (приватный), ветка `main`.

## Найдено и исправлено при аудите прода (15.09)

### Service worker ломал показ обновлений

Старый `public/sw.js` кэшировал `index.html` по принципу «сначала кэш» и содержал
захардкоженные имена бандлов прошлой сборки. После любого деплоя браузер отдавал
пользователю старый `index.html`, тот запрашивал уже несуществующие JS/CSS —
и приложение показывало пустой экран. Новое поведение:

- навигация и `index.html` — всегда сеть, кэш только как офлайн-запас;
- `/assets/*` — можно кэшировать (хэш содержимого в имени файла);
- `/api` и `/uploads` — не кэшируются никогда: это данные конкретных пользователей;
- `install` не падает из-за отсутствия одного файла, `activate` удаляет старые кэши
  и вызывает `clients.claim()`, чтобы новая версия вступила сразу.

Регистрация SW перенесена в `src/main.jsx` и работает только в production-сборке.
В разработке SW, наоборот, снимается, а кэши вычищаются — иначе правки «не применялись».

### Админка была доступна без входа

`/admin` открывался напрямую по URL: проверки сессии не было, а экраны при 401
дорисовывали демо-данные. Теперь `AdminLayout` сверяет токен с сервером через
`GET /api/admin/me` и при отказе редиректит на `/admin/login`. Локальный PIN
больше не заменяет серверную проверку: принимается только после подтверждения
email/пароля в этой сессии и хранится как SHA-256, а не открытым текстом.
Выход теперь действительно удаляет `admin_token`.

### Профиль жил только в браузере

Анкета, имя, город, фото и обложка сохранялись в `localStorage`, поэтому на другом
устройстве профиль был пустым, а в свайпах другим показывалась старая запись из базы.
Теперь сервер — источник истины, `localStorage` только кэш. Галерея хранит ссылки
`/uploads/...` вместо base64: двенадцать фото в base64 выбивали квоту `localStorage`,
и приложение ломалось. Заодно `uploadPhoto` читал устаревшее поле токена и уходил
без `Authorization`.

### Размер бандла

Админка вынесена в 16 ленивых чанков (`React.lazy` + `Suspense`): главный бандл
562 → 437 КБ, предупреждение Vite о чанке >500 КБ ушло.

## Почта в личном кабинете (шаг 2, 15.09)

Раньше email можно было указать только при регистрации, и 24 боевых аккаунта
(созданные до обязательной почты) не имели ни почты, ни способа её добавить —
то есть восстановить доступ было нечем.

- **Бэкенд** (`server/src/routes/auth.js`): `POST /api/auth/email/request` и
  `POST /api/auth/email/confirm`, оба под `authenticateToken`. Код — 6 цифр,
  15 минут, максимум 5 попыток, одноразовый; новый запрос гасит предыдущие.
  Таблица `email_change_codes` (`user_id`, `email`, `code`, `attempts`,
  `expires_at`, `used`). Проверяются формат, «эта почта уже привязана» и
  «занята другим аккаунтом» — и при запросе, и повторно при подтверждении.
- **Смена почты требует подтверждения на новом адресе** — иначе доступ к чужому
  аккаунту передавался бы простой записью в поле. `PUT /api/users/:id` email по-
  прежнему не принимает, и админка его тоже не пишет: обходного пути нет.
- **Клиент**: экран `/email` (шаг 1 — адрес, шаг 2 — код, «Изменить адрес /
  отправить ещё раз»), строка «Электронная почта» во в `Настройках` с текущим
  адресом и жёлтое напоминание сверху, если почты нет.
- **Поле E-mail в анкете больше не врёт.** В `ProfileScreen` оно было заполнено
  демо-значением `alex@mail.ru` и не сохранялось никуда: любой пользователь видел
  чужой адрес и считал почту привязанной. Теперь поле показывает реальный
  привязанный email (из `/api/auth/me`) и ссылку «привязать» / «сменить».
- **Доставка не блокирует флоу**: если SMTP ответил ошибкой, `email/request`
  возвращает `delivered: false`, экран показывает предупреждение, но шаг ввода
  кода остаётся — письмо иногда приходит позже, а код живёт свои 15 минут.

### Просрочка кода восстановления (попутный дефект)

`reset-password` сравнивал `expires_at` (JS-строка `2026-09-15T04:31:00.000Z`) с
`datetime('now')` (`2026-09-15 04:59:00`). Из-за разницы разделителей (`T` против
пробела) сравнение строк всегда давало «не просрочен», и код жил до полуночи, а не
15 минут. Теперь срок сравнивается с `new Date().toISOString()` — один формат с
обеих сторон. Закрыто проверками 21 и 25 в смоук-тесте.

## SMS-авторизация удалена (шаг 3, 15.09)

- Из `auth.js` удалены `POST /send-code` и `POST /verify-code` вместе с их
  хелперами (`normalizePhone`, `PHONE_SQL`, `CODE_PHONE_SQL`) — 129 строк.
  `verify-code` создавал аккаунты без email и пароля (`user<хвост номера>`), то
  есть в обход обязательной почты и без возможности восстановления.
- Таблица `verification_codes` больше не создаётся ни `init.js`, ни `migrate.js`.
  В существующих базах она оставлена намеренно: дропать таблицу на проде нечем
  крыть, а в новых проектах её просто не будет.
- В интерфейсе вызовов не было (`grep` по `src/` пуст) — маршрут `/login` и
  регистрация никогда не ходили в СМС. Оба эндпоинта теперь отдают 404
  (проверка 16 в смоук-тесте).

## Известные ограничения

- Сервер не проверяет наличие PIN: барьер только на клиенте (см. «Обязательность
  PIN»). Жёсткая проверка заблокировала бы аккаунты без PIN — нужен grace-период
  или принудительная установка при следующем входе по паролю.
- У 24 боевых аккаунтов почты нет. Теперь они могут привязать её сами (экран
  `/email`), но пока не привязали — восстановление им недоступно.
- Реальной отправки SMS нет (`SMSC_*` в env не подключены к провайдеру). Отправка email
  на проде работает: SMTP-креденшелы в `server/.env` боевого сервера, в логах systemd
  есть доставленные письма (`✅ Email отправлен: …`). В не-продакшене без SMTP-ключей
  `sendEmail` работает в mock-режим и печатает письмо в консоль.
- Платежи — демо-режим: `POST /api/subscriptions` записывает подписку без внешнего эквайринга.
- Экран анкеты (`/profile`, `/questions`) сохраняет черновик локально; ответы уходят в
  `/api/filters/answers`, но базовые поля профиля на сервер не пишутся.
- Часть таблиц админки и соц-данных наполняется сидом для демо.

## Проверка готовности (15.09, аудит авторизации)

| Что | Результат |
| --- | --- |
| git | `main` синхронизирован с `origin/main` (0 вперёд / 0 позади), коммит `25be666` — флоу PIN |
| Сборка веба | `npm run build` — успешно, 54 модуля, главный чанк ~436 КБ (116 КБ gzip) |
| Линт | `npm run lint` — 0 ошибок, только предупреждения `no-unused-vars`/`exhaustive-deps` |
| Синтаксис бэкенда | `node --check` по всем `server/src/**/*.js` — чисто |
| Серверы | dev-фронт `:5175` и API `:3001` отвечают; `/api/health` → `status: ok` |
| Флоу авторизации | `server/scripts/smoke-auth-pin.mjs` — 27/27 проверок на локальном бэкенде (PIN-флоу, привязка email, отсутствие SMS-маршрутов, просрочка кодов) |
| UI-сценарий (клик-тест) | онбординг → «Пропустить» → регистрация (email обязателен) → `/set-pin` → попытка открыть `/menu` без PIN → отбрасывает на `/set-pin` → PIN задан → `/profile` → «Выйти» → экран сразу с шагом «Введите PIN-код для …» → вход по PIN → `/profile` |
| VPS, код | `server/src/routes/auth.js` на сервере совпадает с `HEAD` (md5), `dist` — с локальной сборкой |
| VPS, сервисы | `platformlove-backend` и `nginx` — active; `NODE_ENV=production` (SMS `debug_code` наружу не уходит) |
| VPS, API | регистрация без email → 400; вход несуществующего → нейтральное 401; `forgot-password` → одинаковый ответ для любого логина |
| VPS, схема | в боевой БД есть `users.email`, `users.pin_code`, таблица `password_reset_codes` |
| VPS, данные | 36 пользователей: email — у 12, PIN — у 3, без email — 24 (могут привязать сами через `/email`) |

Миграция схемы лежит в `server/src/db/init.js` (`safeAddColumn` + `CREATE TABLE IF NOT EXISTS`),
она и применялась при деплое; `server/src/db/migrate.js` про email/PIN не знает —
новый код это не ломает, но держать две «миграции» в одном проекте опасно.
